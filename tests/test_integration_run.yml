- name: integration-empty
  tags:
    - integration
  command: >
    snakemake
      --reason
      --printshellcmds
      --jobs 1
      --latency-wait 120
      --use-singularity --singularity-args ' --cleanenv --bind /tmp'
      --config
        pepfile=tests/pep/project_config_empty.yaml
        reference=tests/data/ref.fa
      --snakefile Snakefile
  files:
    - path: sample1/combinations/0_A.vcf.gz
    - path: sample1/combinations/0_B.vcf.gz
    - path: sample1/sample1_no_homref.vcf
    - path: sample1/fasta/0_A_all.fasta
    - path: sample1/fasta/0_B_all.fasta
    - path: sample1/fasta_files.txt
      contains:
        - 0_A_all.fasta
        - 0_B_all.fasta

- name: integration-unphased
  tags:
    - integration
  command: >
    snakemake
      --reason
      --printshellcmds
      --jobs 1
      --latency-wait 120
      --use-singularity --singularity-args ' --cleanenv --bind /tmp'
      --config pepfile=tests/pep/project_config_unphased.yaml
      --snakefile Snakefile
  files:
    - path: sample2/combinations/0_A.vcf
    - path: sample2/combinations/0_B.vcf

- name: integration-phased
  tags:
    - integration
  command: >
    snakemake
      --reason
      --printshellcmds
      --jobs 1
      --latency-wait 120
      --use-singularity --singularity-args ' --cleanenv --bind /tmp'
      --config pepfile=tests/pep/project_config_phased.yaml
      --snakefile Snakefile
  files:
    - path: sample3/combinations/0_A.vcf
    - path: sample3/combinations/1_A.vcf
    - path: sample3/combinations/0_B.vcf
    - path: sample3/combinations/1_B.vcf
